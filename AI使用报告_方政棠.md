# 《数据分析与可视化》期末考试——AI 辅助使用报告

**学生**：方政棠 | 学号：032402134 | 专业：经济统计学

---

## 一、AI 工具使用概述

在本课程期末考试的数据分析项目中，我借助了 AI 大语言模型作为辅助工具，用于**代码调试、统计方法咨询、可视化调优和结论论证**等环节。以下选取项目中若干难度较高、关键性较强的部分，详细说明 AI 如何参与辅助以及我自身的思考与决策过程。

| 使用环节 | AI 辅助内容 | 难度评级 |
|---|---|---|
| 正则表达式数据清洗 | 正则编写与调试 | ★★★★ |
| 辛普森悖论的识别与分析 | 统计诊断与交叉验证 | ★★★★★ |
| numpy 多项式拟合趋势线 | 方法选型与参数调试 | ★★★ |
| 可视化图表美学调优 | 配色、标注、布局优化 | ★★★ |
| 分析结论的逻辑验证 | 论证链完整性与因果推断 | ★★★★ |

---

## 二、重点环节详述

### 2.1 正则表达式批量清洗文本字段

**任务描述**：原始 CSV 数据中，`面积`（如 `103㎡`）、`总价`（如 `199万`）、`均价`（如 `19321元/㎡`）、`时间`（如 `2025年建造`）、`楼层`（如 `中层(共31层)`）、`户型`（如 `3 室 2 厅 2 卫`）六个关键字段均为带中文单位和符号的文本格式，无法直接用于数值计算。虽然 CSV 中已有预计算的数值列，但为了体现完整的数据处理能力，我选择从原始文本自行提取数值。

**AI 辅助过程**：

1. **正则表达式编写与调试**：向 AI 描述了每个字段的格式特征，AI 给出了对应的正则表达式建议。例如：
   - 面积提取 `([\d.]+)` —— 匹配 `103` 或 `103.5` 中的数字
   - 楼层提取分两步：先提取类别 `(低层|中层|高层)`，再提取总层数 `共(\d+)层`
   - 户型提取分三步：`(\d+)\s*室`、`(\d+)\s*厅`、`(\d+)\s*卫`

2. **边界情况排查**：我向 AI 提出了几个潜在问题——"如果户型字段中出现非标准格式怎么办？"、"如果总价有小数如 180.9 万怎么办？"。AI 帮助我确认了 `([\d.]+)` 可以同时匹配整数和小数，并建议在清洗后通过 `describe()` 检查数值范围来验证提取正确性。

3. **分类字段标准化的 where/isin 逻辑**：AI 建议使用 `data['字段'].where(data['字段'].isin(标准值列表))` 的模式对方位和房龄字段进行格式验证，将任何非标准值自动标记为 NaN，避免后续分析被污染。

**我的贡献**：最终的正则表达式选择和清洗策略（如是否处理缺失值、是否保留原始列）由我根据数据实际情况和分析需求独立决定。所有清洗代码的嵌入位置和输出验证由我完成。

---

### 2.2 辛普森悖论的识别与交叉分析设计

**任务描述**：在按房龄分组统计均价时，出现了一个反直觉的结果——"2年内"次新房的整体均价（18,011 元/㎡）反而是四个房龄段中**最低**的，而"10年以上"老房均价（19,695 元/㎡）最高。如果简单地下结论说"福州新房比老房便宜"，那就犯了一个典型的统计错误。

**AI 辅助过程**：

1. **异常结果诊断**：当我将房龄分组统计结果提供给 AI 时，AI 立即指出了这可能是**辛普森悖论（Simpson's Paradox）**——即分组数据中的趋势在合并数据中发生逆转，通常由**混杂变量**（此处为地段/市区）导致。AI 解释了其机制：大量次新房集中在外围低价区域拉低了整体均值，而大量老旧房源集中在核心高价位区域抬高了整体均值。

2. **交叉分析方案设计**：AI 建议通过 `pivot_table` 构建"市区 × 房龄"的二维透视表来**控制地段变量**，从而揭示房龄的真实效应。具体方法为：
   - 以 `市区` 为行索引、`房龄_clean` 为列索引
   - 以 `均价_元每平` 的均值为聚合值
   - 横向比较同一市区不同房龄的价格变化（控制地段后的房龄效应）
   - 纵向比较同一房龄不同市区的价格差异（验证地段效应）

3. **热力图可视化验证**：AI 建议将透视表结果用 `sns.heatmap` 可视化，使得"地段 × 房龄"的交互效应一目了然。热力图清晰地展示了：鼓楼区"2年内"→"10年以上"的均价从 32,780 骤降至 23,488 元/㎡，呈现显著的房龄负效应；而晋安区四个房龄段均价几乎持平，房龄效应不明显。

**我的贡献**：
- 辛普森悖论的具体数值验证由我独立完成（§6.3 和 §6.5 的表格）
- 在结论部分（§8.3），我结合福州城市的实际情况——核心区土地稀缺、学区集中——对统计现象进行了经济学解释
- 我判断这一发现是报告的核心亮点之一，因此将其置于摘要和结论的显著位置

---

### 2.3 numpy 多项式拟合趋势线

**任务描述**：在面积-总价散点图（图表 4）中，需要添加一条整体线性趋势线来直观展示两变量的正相关关系，同时标注 Pearson 相关系数。

**AI 辅助过程**：

1. **方法选型**：我最初考虑使用 `seaborn` 的 `regplot` 直接绘制，但发现 `regplot` 无法与按房龄分色的多组散点图兼容。向 AI 咨询后，AI 推荐使用 `numpy.polynomial.polynomial.polyfit` 手动拟合，再用 `plot` 叠加趋势线。

2. **代码实现**：AI 给出了以下关键代码片段：
   ```python
   from numpy.polynomial.polynomial import polyfit
   b, m = polyfit(x_fit, y_fit, 1)
   x_line = np.linspace(x_fit.min(), x_fit.max(), 100)
   ax.plot(x_line, b + m * x_line, 'k--', linewidth=1.5)
   ```

3. **结果解读**：我向 AI 提供了 Pearson r = 0.64 的结果，AI 帮助解读了为什么相关系数未达 0.8 以上——面积虽是总价的重要决定因素，但地段、房龄等其他变量的综合影响权重不亚于面积本身，这恰好说明了福州市场的多维定价特征。

**我的贡献**：数据预处理（移除缺失值、限制 x 轴范围）、散点图的按组着色逻辑、趋势线样式（黑色虚线）等均由我独立决策。

---

### 2.4 可视化图表美学调优

**任务描述**：报告需要 6 张可视化图表，要求呈现专业、美观、信息密集的视觉效果，包括合理的配色方案、清晰的标注和专业的排版。

**AI 辅助过程**：

1. **配色方案建议**：AI 提供了多套配色方案供选择：
   - 柱状图：核心四区用 `#2E86AB`（蓝）、外围区县用 `#A23B72`（紫），以颜色区分供给主力与外围
   - 房龄分组：从红→橙→蓝→灰的渐变色系（`#D64045` → `#E8913A` → `#2E86AB` → `#7D8C91`），暗示"新房→旧房"的视觉层次
   - 热力图：`RdYlGn_r`（红绿反转色板），红色低值、绿色高值

2. **标注布局优化**：柱状图上方的数量+占比标注、箱线图中的中位数文字标注、误差线柱状图中的均值数值标注——AI 帮助调整了文字位置偏移量和字体大小，避免标注之间相互重叠。

3. **中文字体配置**：针对 Windows 环境下 matplotlib 中文显示乱码问题，AI 给出了标准解决方案：
   ```python
   plt.rcParams['font.sans-serif'] = ['SimHei', 'Microsoft YaHei']
   plt.rcParams['axes.unicode_minus'] = False
   ```

**我的贡献**：每张图表所回答的分析问题、图表类型的选择（柱状/直方/箱线/散点/热力）、以及图表在报告中的布局位置，均由我根据分析目标独立设计。

---

### 2.5 分析结论的逻辑验证与深化

**任务描述**：在完成统计分析和可视化后，需要提炼出有洞察力的结论，并确保论证链条的严密性——数据分析中常见的陷阱包括：相关性误作因果性、忽略混杂变量、选择性报告有利结果等。

**AI 辅助过程**：

1. **"数据质量反思"部分的建议**：我主动向 AI 提问——"这份分析有哪些潜在局限？"AI 帮助梳理了以下方面：
   - 数据来源单一（仅安居客平台，不同平台挂牌价可能存在系统偏差）
   - 挂牌价 ≠ 成交价的警示
   - 关键缺失变量（学区、地铁距离、装修程度）
   - 建造年份中"2026 年建造"可能为期房的歧义

2. **论证链完整性检查**：我将初稿结论提交给 AI，要求其从"每个结论是否有数据支撑"的角度进行检验。AI 指出了每一条结论所对应的数据来源（图表编号和章节），帮助我建立了"结论→数据"的双向映射关系。

3. **辛普森悖论的警示价值**：AI 强调了对这一统计现象的发现不应仅停留在"找到了一个反直觉结果"的层面，而应提炼为**数据分析的一般性原则**——"看到分组统计结果时，应当本能地问一句：有没有第三个变量在起混淆作用？"我将此写入了 §8.5 的反思部分。

**我的贡献**：
- 所有结论的主线和支线结构由我独立设计
- 结合福州城市的实际情况（东拓南进战略、学区分布、地铁网络）对统计发现进行情境化解读
- 最终的文字表达、排版组织和信息密度控制由我完成

---

## 三、AI 使用原则总结

在本次项目中，我对 AI 的使用遵循了以下原则：

| 原则 | 说明 |
|---|---|
| **辅助而非替代** | AI 提供方法建议和代码片段，但最终的分析框架、变量选择和结论判断由我独立完成 |
| **验证而非盲从** | AI 给出的每段代码都经过我逐行运行验证，确保输出符合预期 |
| **追问而非接受** | 当 AI 给出初始结果后，我会追问"为什么"、"还有什么可能的问题"以深入挖掘 |
| **理解而非复制** | 使用 AI 建议的方法前，我会确保自己理解了其原理（如正则表达式语法、辛普森悖论的机制） |

AI 工具在本次项目中最大的价值体现于：**加速了代码调试周期**（尤其是正则表达式的试错过程）、**提供了统计方法论的视角补充**（辛普森悖论的诊断思路）、以及**作为一位"永不疲倦的审稿人"**对我的分析逻辑进行推敲。但项目的核心智力活动——包括分析框架的设计、变量的经济学解读、以及将统计结果转化为有洞察力结论的能力——仍然需要人类分析师的知识储备和批判性思维。

---

